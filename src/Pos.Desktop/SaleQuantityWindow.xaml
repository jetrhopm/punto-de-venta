<Window x:Class="Pos.Desktop.SaleQuantityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cantidad de venta" Width="470" Height="330" MinWidth="470" MinHeight="330"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Cantidad de la partida" FontSize="22" FontWeight="SemiBold" Foreground="#17324D"/>
            <TextBlock x:Name="ProductText" Margin="0,6,0,0" Foreground="#526577" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="0,22,0,0">
            <TextBlock Text="Cantidad" FontWeight="SemiBold"/>
            <TextBox x:Name="QuantityBox" Height="42" Margin="0,6,0,0" FontSize="18" VerticalContentAlignment="Center"/>
            <Border Margin="0,13,0,0" Padding="12" Background="#EEF6FB" BorderBrush="#C9E0EF" BorderThickness="1" CornerRadius="5">
                <TextBlock x:Name="UnitHintText" Foreground="#24516D" TextWrapping="Wrap"/>
            </Border>
            <TextBlock x:Name="MessageText" Margin="0,12,0,0" Foreground="#B42318" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="116" Height="39" Margin="0,0,10,0" Background="#C63D3D" Foreground="White" IsCancel="True">Cancelar</Button>
            <Button Width="132" Height="39" Background="#238A68" Foreground="White" FontWeight="SemiBold" IsDefault="True" Click="OnSaveClick">Guardar cantidad</Button>
        </StackPanel>
    </Grid>
</Window>
