<Window x:Class="Pos.Desktop.OperationConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Width="520" MinHeight="300" MaxHeight="700" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Border x:Name="IconPanel" Width="54" Height="54" CornerRadius="6" BorderThickness="1">
                <icons:PackIconMaterial x:Name="ResultIcon" Width="31" Height="31" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,2,0,0"><TextBlock x:Name="TitleText" FontSize="22" FontWeight="Bold"/><TextBlock Text="Confirma la acción antes de continuar" Margin="0,3,0,0" Foreground="{StaticResource TextMutedBrush}"/></StackPanel>
        </StackPanel>
        <Border Grid.Row="1" Margin="0,20,0,16" Padding="14" Background="#F3F8FC" BorderBrush="#C8DCE9" BorderThickness="1" CornerRadius="5"><ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><TextBlock x:Name="MessageText" TextWrapping="Wrap"/></ScrollViewer></Border>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Width="130" Margin="0,0,8,0" Style="{StaticResource CancelButtonStyle}" IsCancel="True" Click="OnCancelClick">Cancelar</Button>
            <Button x:Name="ConfirmButton" Width="150" Style="{StaticResource ConfirmButtonStyle}" IsDefault="True" Click="OnConfirmClick">Confirmar</Button>
        </StackPanel>
    </Grid>
</Window>
