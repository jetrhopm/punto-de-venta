<Window x:Class="Pos.Desktop.ShiftCloseSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Corte realizado" Width="460" Height="330"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Corte realizado" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
        <StackPanel Grid.Row="1" Margin="0,22,0,12">
            <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Efectivo esperado en caja" /><TextBlock x:Name="ExpectedText" Grid.Column="1" FontWeight="SemiBold" /></Grid>
            <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Efectivo contado por el usuario" /><TextBlock x:Name="CountedText" Grid.Column="1" FontWeight="SemiBold" /></Grid>
            <Separator Margin="0,0,0,14" />
            <Grid><Grid.ColumnDefinitions><ColumnDefinition /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="Diferencia" FontWeight="Bold" /><TextBlock x:Name="DifferenceText" Grid.Column="1" FontSize="20" FontWeight="Bold" /></Grid>
        </StackPanel>
        <Button Grid.Row="2" Content="Continuar" Height="40" Style="{StaticResource PrimaryButtonStyle}" Click="OnContinueClick" />
    </Grid>
</Window>
