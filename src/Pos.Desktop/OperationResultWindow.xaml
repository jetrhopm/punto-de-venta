<Window x:Class="Pos.Desktop.OperationResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:icons="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Width="500" MinHeight="280" MaxHeight="700" SizeToContent="Height" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="28">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Border x:Name="IconPanel" Width="54" Height="54" CornerRadius="6" BorderThickness="1">
                <icons:PackIconMaterial x:Name="ResultIcon" Width="31" Height="31" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel Margin="14,2,0,0"><TextBlock x:Name="TitleText" FontSize="22" FontWeight="Bold"/><TextBlock x:Name="SubtitleText" Margin="0,3,0,0" Foreground="{StaticResource TextMutedBrush}"/></StackPanel>
        </StackPanel>
        <Border Grid.Row="1" Margin="0,20,0,16" Padding="14" Background="#F3F8FC" BorderBrush="#C8DCE9" BorderThickness="1" CornerRadius="5"><ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><TextBlock x:Name="MessageText" TextWrapping="Wrap"/></ScrollViewer></Border>
        <Button Grid.Row="2" Width="130" HorizontalAlignment="Right" Style="{StaticResource ConfirmButtonStyle}" IsDefault="True" Click="OnCloseClick">Aceptar</Button>
    </Grid>
</Window>
