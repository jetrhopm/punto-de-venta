# JetVenta 3.2.10 - Orden de navegación

Se intercambiaron visualmente los accesos de la barra superior:

- `Historial` ocupa la posición previa de `Config.`.
- `Config.` queda como el último acceso, donde antes estaba `Historial`.

No cambian comandos, permisos ni flujos de navegación.
